# 🗄️ Phase 10: Supabase Database Integration - Setup Guide

## 📋 Vue d'ensemble

Supabase est une alternative Firebase open-source basée sur PostgreSQL. C'est parfait pour notre portfolio car:
- ✅ PostgreSQL hébergé dans le cloud
- ✅ API REST/GraphQL automatique (pas de backend à coder!)
- ✅ Gratuit avec limites généreuses
- ✅ Authentication intégrée
- ✅ Real-time capabilities
- ✅ Parfait pour Vercel + Vanilla JavaScript

---

## 🚀 Étape 1: Créer un compte Supabase

### 1. Aller sur https://supabase.com/
### 2. Cliquer "Start your project"
### 3. Créer un compte avec:
   - **Email:** Mikealladoum@gmail.com
   - **Password:** À définir (sauvegarde dans un gestionnaire!)
   - **Région:** Europe (pour la latence basse)

### 4. Créer une nouvelle organisation
   - **Name:** Mike Alladoum Portfolio
   - **Plan:** Free Tier (gratuit)

### 5. Créer un nouveau projet
   - **Name:** porto-database
   - **Database Password:** À générer (Supabase le génère automatiquement)
   - **Region:** Europe (nearest to you)
   - **Pricing Plan:** Free

---

## 🔑 Étape 2: Récupérer les Clés d'Authentification

Après création du projet:

1. Aller à **Settings** → **API**
2. Copier ces 2 valeurs:
   - **`SUPABASE_URL`** - L'URL de base (ex: `https://xxxxx.supabase.co`)
   - **`SUPABASE_ANON_KEY`** - La clé anonyme publique

3. **NE JAMAIS** partager `SUPABASE_SERVICE_ROLE_KEY` (clé administrative)

**Format à sauvegarder:**
```
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

## 🛢️ Étape 3: Créer les Tables en Base de Données

Dans Supabase Dashboard:

### 1. Aller à **SQL Editor**
### 2. Créer une nouvelle query avec ce script SQL:

```sql
-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Projects Table
CREATE TABLE projects (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()),
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  tech TEXT[] DEFAULT '{}',
  github TEXT,
  live TEXT,
  featured BOOLEAN DEFAULT false,
  archived BOOLEAN DEFAULT false
);

-- Certifications Table
CREATE TABLE certifications (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()),
  title TEXT NOT NULL,
  issuer TEXT NOT NULL,
  year INTEGER NOT NULL,
  description TEXT NOT NULL,
  url TEXT,
  archived BOOLEAN DEFAULT false
);

-- Education Table
CREATE TABLE education (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()),
  title TEXT NOT NULL,
  institution TEXT NOT NULL,
  start_year INTEGER NOT NULL,
  end_year TEXT,
  description TEXT NOT NULL,
  archived BOOLEAN DEFAULT false
);

-- CVs Table
CREATE TABLE cvs (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()),
  filename TEXT NOT NULL,
  size INTEGER NOT NULL,
  upload_date TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()),
  content_type TEXT NOT NULL,
  base64 TEXT NOT NULL,
  active BOOLEAN DEFAULT false
);

-- Testimonials Table
CREATE TABLE testimonials (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()),
  author TEXT NOT NULL,
  text TEXT NOT NULL,
  status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'rejected')),
  archived BOOLEAN DEFAULT false
);

-- Analytics Table
CREATE TABLE analytics (
  id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()),
  page_views INTEGER DEFAULT 0,
  cv_downloads INTEGER DEFAULT 0,
  unique_visitors INTEGER DEFAULT 0,
  session_duration TEXT DEFAULT '0m 0s',
  monthly_activity JSONB DEFAULT '[]'::jsonb,
  last_updated TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW())
);

-- Enable Row Level Security (RLS)
ALTER TABLE projects ENABLE ROW LEVEL SECURITY;
ALTER TABLE certifications ENABLE ROW LEVEL SECURITY;
ALTER TABLE education ENABLE ROW LEVEL SECURITY;
ALTER TABLE cvs ENABLE ROW LEVEL SECURITY;
ALTER TABLE testimonials ENABLE ROW LEVEL SECURITY;
ALTER TABLE analytics ENABLE ROW LEVEL SECURITY;

-- Create RLS Policies (Allow PUBLIC READ, only authenticated users can WRITE)
-- Projects
CREATE POLICY "Projects are publicly readable" ON projects FOR SELECT USING (true);
CREATE POLICY "Authenticated users can insert projects" ON projects FOR INSERT WITH CHECK (auth.role() = 'authenticated');
CREATE POLICY "Authenticated users can update projects" ON projects FOR UPDATE USING (auth.role() = 'authenticated');
CREATE POLICY "Authenticated users can delete projects" ON projects FOR DELETE USING (auth.role() = 'authenticated');

-- Certifications
CREATE POLICY "Certifications are publicly readable" ON certifications FOR SELECT USING (true);
CREATE POLICY "Authenticated users can insert certifications" ON certifications FOR INSERT WITH CHECK (auth.role() = 'authenticated');
CREATE POLICY "Authenticated users can update certifications" ON certifications FOR UPDATE USING (auth.role() = 'authenticated');
CREATE POLICY "Authenticated users can delete certifications" ON certifications FOR DELETE USING (auth.role() = 'authenticated');

-- Education
CREATE POLICY "Education is publicly readable" ON education FOR SELECT USING (true);
CREATE POLICY "Authenticated users can insert education" ON education FOR INSERT WITH CHECK (auth.role() = 'authenticated');
CREATE POLICY "Authenticated users can update education" ON education FOR UPDATE USING (auth.role() = 'authenticated');
CREATE POLICY "Authenticated users can delete education" ON education FOR DELETE USING (auth.role() = 'authenticated');

-- CVs
CREATE POLICY "CVs are publicly readable" ON cvs FOR SELECT USING (true);
CREATE POLICY "Authenticated users can insert cvs" ON cvs FOR INSERT WITH CHECK (auth.role() = 'authenticated');
CREATE POLICY "Authenticated users can update cvs" ON cvs FOR UPDATE USING (auth.role() = 'authenticated');
CREATE POLICY "Authenticated users can delete cvs" ON cvs FOR DELETE USING (auth.role() = 'authenticated');

-- Testimonials
CREATE POLICY "Testimonials are publicly readable" ON testimonials FOR SELECT USING (true);
CREATE POLICY "Authenticated users can insert testimonials" ON testimonials FOR INSERT WITH CHECK (auth.role() = 'authenticated');
CREATE POLICY "Authenticated users can update testimonials" ON testimonials FOR UPDATE USING (auth.role() = 'authenticated');
CREATE POLICY "Authenticated users can delete testimonials" ON testimonials FOR DELETE USING (auth.role() = 'authenticated');

-- Analytics
CREATE POLICY "Analytics are publicly readable" ON analytics FOR SELECT USING (true);
CREATE POLICY "Authenticated users can update analytics" ON analytics FOR UPDATE USING (auth.role() = 'authenticated');
```

### 3. Exécuter la query

---

## 🔐 Étape 4: Configurer l'Authentication

Dans Supabase Dashboard:

1. Aller à **Authentication** → **Settings**
2. Copier le **JWT Secret** (pour plus tard)
3. En **Email/Password**, activer:
   - ✅ Enable email confirmations
   - ✅ Autoconfirm new users (pour testing)

### Créer un utilisateur admin:

1. Aller à **Authentication** → **Users**
2. Cliquer **"Invite user"**
3. Email: `admin@portfolio.dev`
4. Password: `Admin123!Secure`
5. Inviter

---

## 📁 Étape 5: Intégration JavaScript

### Fichier: `public/js/supabase-config.js`

```javascript
// Supabase Configuration
const SUPABASE_URL = 'https://YOUR_PROJECT_ID.supabase.co';
const SUPABASE_ANON_KEY = 'YOUR_ANON_KEY_HERE';

// Initialize Supabase Client
const { createClient } = supabase;
const supabaseClient = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

// Check authentication status
async function checkAuth() {
    const { data } = await supabaseClient.auth.getSession();
    return data.session;
}

// Export for use in dashboard
window.supabaseClient = supabaseClient;
```

### Charger dans `dashboard.html`:

```html
<!-- Add before closing body tag -->
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="/js/supabase-config.js"></script>
```

---

## 🔄 Étape 6: Migrer les Données

Après configuration, nous allons:

1. ✅ Garder localStorage comme cache local
2. ✅ Synchroniser vers Supabase au démarrage
3. ✅ Mettre à jour Supabase à chaque modification
4. ✅ Charger depuis Supabase au reload

---

## ✅ Checklist Setup:

- [ ] Compte Supabase créé (Mikealladoum@gmail.com)
- [ ] Projet `porto-database` créé
- [ ] Tables créées via SQL
- [ ] Row Level Security activé
- [ ] Utilisateur admin créé (admin@portfolio.dev)
- [ ] `SUPABASE_URL` et `SUPABASE_ANON_KEY` copiés
- [ ] Clés sauvegardées dans `.env.local`

---

## 🚨 Important - Variables d'Environnement

Créer un fichier `.env.local` (NE PAS committer!):

```
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

Sur Vercel, ajouter ces variables dans:
**Settings** → **Environment Variables**

---

## 📞 Support

- Supabase Docs: https://supabase.com/docs
- JavaScript Client: https://supabase.com/docs/reference/javascript/introduction
- Row Level Security: https://supabase.com/docs/guides/auth/row-level-security

---

## 🎯 Prochaines Étapes:

Une fois que tu as complété ce setup, je vais:

1. Créer `supabase-config.js` avec ta clé
2. Intégrer Supabase au dashboard
3. Migrer les données
4. Tester la synchronisation
5. Déployer sur Vercel

**Fais-moi signe quand tu as complété le setup Supabase!** ✅
